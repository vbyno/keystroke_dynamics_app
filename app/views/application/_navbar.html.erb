<nav class="navbar navbar-expand-lg navbar-light bg-light">
<%= link_to 'Keystroke Fingerprint', new_keystroke_session_path, class: 'navbar-brand' %>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarSupportedContent">
  <ul class="navbar-nav mr-auto">
    <li class="nav-item active">
      <a class="nav-link" href="#">Input text<span class="sr-only">(current)</span></a>
    </li>
  </ul>
  <form class="form-inline my-2 my-lg-0">
    <% if user_signed_in? %>
      <span style="margin-right: 20px;">
        <%= current_user.name %>
      </span>
      <%= link_to 'Logout', destroy_user_session_path, method: :delete, class: 'btn btn-outline-danger my-2 my-sm-0' %>
    <% else %>
      <%= link_to 'Login', new_user_session_path, class: 'btn btn-outline-success my-2 my-sm-0' %>
    <% end %>
  </form>
</nav>
